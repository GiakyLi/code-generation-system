# /.env.example

# --- 日志配置 ---
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# --- Temporal 配置 ---
TEMPORAL_SERVER=temporal:7233
TASK_QUEUE=code-generation-task-queue

# --- 服务间通信URL ---
# 注意: 这些URL现在使用Docker Compose的服务名, 而不是localhost或host.docker.internal
# 这是更健壮的网络配置方式。
SANDBOX_URL=http://sandbox:8000

# --- 大语言模型(LLM)端点 ---
# 在真实场景中, 这些应该是指向VLLM或其他推理服务的真实URL。
# 并且相关的API密钥应该通过安全的秘密管理工具提供。
# 注意: host.docker.internal 是从Docker容器内部访问宿主机的特殊DNS名称
VLLM_MODEL_A_URL=[http://host.docker.internal:8001/generate](http://host.docker.internal:8001/generate)
VLLM_MODEL_B_URL=[http://host.docker.internal:8002/generate](http://host.docker.internal:8002/generate)

# --- UI配置 ---
# Streamlit UI连接Temporal前端时需要访问的地址。
# 从UI容器内部, 它通过`temporal-net`网络访问`temporal`服务。
# 注意: 这是服务间通信的地址。
UI_TEMPORAL_SERVER=temporal:7233

# --- 沙箱配置 ---
# 关键安全配置: 指向隔离的Docker-in-Docker (DinD) 守护进程
DOCKER_HOST=tcp://dind:2375
# 沙箱内部用于执行测试的Docker镜像标签。
# 这个标签应与docker-compose.yml中构建的镜像名称一致。
SANDBOX_TEST_IMAGE_TAG=test-execution-env:latest
# 沙箱中执行代码的超时时间(秒)。
SANDBOX_EXECUTION_TIMEOUT=60